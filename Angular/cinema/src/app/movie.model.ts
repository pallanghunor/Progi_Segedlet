export interface MovieModel {
    id: number
    title: string
    star: string
    description: string
    onAir: string
    imageUrl: string,

    from: string;
    to: string;
    imageBase64: string;
}